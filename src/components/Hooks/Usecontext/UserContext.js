//Step one is to create the context
import React from 'react'

const UserContext = React.createContext();
// After creating our context react automatically gives us a provider

export default UserContext